{
  "previewPort": 3000,
  "healthPath": "/",
  "startCommand": "docker compose -f docker-compose.base44.yml up -d",
  "secrets": [
    {
      "name": "ANTHROPIC_API_KEY",
      "description": "Anthropic API key for the AI ensemble inspector. Optional — app runs with a stub inspector when blank.",
      "requiredAtBoot": false
    },
    {
      "name": "OPENAI_API_KEY",
      "description": "OpenAI API key for the AI ensemble inspector. Optional — app runs with a stub inspector when blank.",
      "requiredAtBoot": false
    },
    {
      "name": "GOOGLE_API_KEY",
      "description": "Google AI API key for the AI ensemble inspector. Optional — app runs with a stub inspector when blank.",
      "requiredAtBoot": false
    }
  ]
}
