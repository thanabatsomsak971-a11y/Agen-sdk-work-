{
  "previewPort": 3000,
  "healthPath": "/",
  "startCommand": "docker compose -f docker-compose.base44.yml up -d",
  "secrets": [
    {
      "name": "ANTHROPIC_API_KEY",
      "description": "Anthropic API key for AI inspection (optional, leave blank to run in stub mode)",
      "requiredAtBoot": false
    },
    {
      "name": "OPENAI_API_KEY",
      "description": "OpenAI API key for AI inspection (optional, leave blank to run in stub mode)",
      "requiredAtBoot": false
    }
  ]
}
