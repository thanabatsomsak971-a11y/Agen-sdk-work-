# Local dev placeholders — app boots without external credentials.
# Real secrets (when provided) are delivered via /run/base44/app.env and override these.
JWT_SECRET=dev-only-secret-change-in-production-please-min16
ANTHROPIC_API_KEY=
OPENAI_API_KEY=
GOOGLE_API_KEY=
